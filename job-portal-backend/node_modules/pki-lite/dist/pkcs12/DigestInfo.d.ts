import { OctetString } from '../asn1/OctetString.js';
import { AlgorithmIdentifier, DigestAlgorithmIdentifier } from '../algorithms/AlgorithmIdentifier.js';
import { Asn1BaseBlock, PkiBase } from '../core/PkiBase.js';
/**
 * Represents a DigestInfo structure in a PKCS#12 file.
 *
 * @asn
 * ```asn
 * DigestInfo ::= SEQUENCE {
 *   digestAlgorithm DigestAlgorithmIdentifier,
 *   digest OCTET STRING
 * }
 * ```
 */
export declare class DigestInfo extends PkiBase<DigestInfo> {
    digestAlgorithm: DigestAlgorithmIdentifier;
    digest: OctetString;
    constructor(options: {
        digestAlgorithm: AlgorithmIdentifier;
        digest: OctetString | Uint8Array<ArrayBuffer>;
    });
    toAsn1(): Asn1BaseBlock;
    static fromAsn1(asn1: Asn1BaseBlock): DigestInfo;
    static fromDer(der: Uint8Array<ArrayBuffer>): DigestInfo;
}

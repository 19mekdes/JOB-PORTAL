import { ContentInfo } from '../pkcs7/ContentInfo.js';
import { Asn1BaseBlock, PkiSequence } from '../core/PkiBase.js';
import { SafeBag } from './SafeBag.js';
import { OIDs } from '../core/OIDs.js';
/**
 * Represents the AuthenticatedSafe structure described in PKCS#12 standard.
 *
 * @asn
 * ```asn
 * AuthenticatedSafe ::= SEQUENCE OF ContentInfo
 * ```
 */
export declare class AuthenticatedSafe extends PkiSequence<ContentInfo> {
    static fromAsn1(asn1: Asn1BaseBlock): AuthenticatedSafe;
    static fromDer(der: Uint8Array<ArrayBuffer>): AuthenticatedSafe;
    getBags(password: string | Uint8Array<ArrayBuffer>): Promise<SafeBag[]>;
    getBagsByName(bagName: keyof typeof OIDs.PKCS12.BAGS, password: string | Uint8Array<ArrayBuffer>): Promise<SafeBag[]>;
}

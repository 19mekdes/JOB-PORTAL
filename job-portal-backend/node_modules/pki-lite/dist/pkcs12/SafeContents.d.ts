import { Asn1BaseBlock, PkiSequence } from '../core/PkiBase.js';
import { SafeBag } from './SafeBag.js';
/**
 * Represents the SafeContents structure in a PKCS#12 file.
 *
 * @asn
 * ```asn
 * SafeContents ::= SEQUENCE OF SafeBag
 * ```
 */
export declare class SafeContents extends PkiSequence<SafeBag> {
    static fromAsn1(asn1: Asn1BaseBlock): SafeContents;
    static fromDer(der: Uint8Array<ArrayBuffer>): SafeContents;
}

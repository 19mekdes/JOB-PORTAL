import { AlgorithmIdentifier, DigestAlgorithmIdentifier } from '../algorithms/AlgorithmIdentifier.js';
import { OctetString } from '../asn1/OctetString.js';
import { asn1js, PkiBase } from '../core/PkiBase.js';
/**
 * Represents PBKDF2-params structure.
 *
 * @asn
 * ```asn
 * PBKDF2-params ::= SEQUENCE {
 *   salt CHOICE {
 *     specified OCTET STRING,
 *     otherSource AlgorithmIdentifier {{PBKDF2-SaltSources}}
 *   },
 *   iterationCount INTEGER (1..MAX),
 *   keyLength INTEGER (1..MAX) OPTIONAL,
 *   prf AlgorithmIdentifier {{PBKDF2-PRFs}} DEFAULT
 *     algid-hmacWithSHA1
 * }
 * ```
 */
export declare class PBKDF2Params extends PkiBase<PBKDF2Params> {
    salt: OctetString | AlgorithmIdentifier;
    iterationCount: number;
    keyLength?: number;
    prf: DigestAlgorithmIdentifier;
    constructor(options: {
        salt: Uint8Array<ArrayBuffer> | AlgorithmIdentifier | OctetString;
        iterationCount: number;
        keyLength?: number;
        prf?: AlgorithmIdentifier;
    });
    toAsn1(): asn1js.Sequence;
    static fromAsn1(asn1: asn1js.BaseBlock): PBKDF2Params;
}

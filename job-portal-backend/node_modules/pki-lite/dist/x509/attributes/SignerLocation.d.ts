import { Asn1BaseBlock, PkiBase, PkiSequence } from '../../core/PkiBase.js';
import { DirectoryString } from '../DirectoryString.js';
/**
 * Represents a postal address.
 *
 * @asn
 * ```asn
 * PostalAddress ::= SEQUENCE SIZE(1..6) OF DirectoryString
 * ```
 */
export declare class PostalAddress extends PkiSequence<DirectoryString> {
    protected maxSize?: number;
    static fromAsn1(asn1: Asn1BaseBlock): PostalAddress;
}
/**
 * SignerLocation Attribute
 *
 * @asn
 * ```asn
 * SignerLocation ::= SEQUENCE {
 *       -- at least one of the following must be present
 *      countryName          [0] DirectoryString      OPTIONAL,
 *                            -- as used to name a Country in X.500
 *      localityName         [1] DirectoryString      OPTIONAL,
 *                            -- as used to name a locality in X.500
 *      postalAdddress       [2] PostalAddress        OPTIONAL
 * }
 * ```
 */
export declare class SignerLocation extends PkiBase<SignerLocation> {
    countryName?: DirectoryString;
    localityName?: DirectoryString;
    postalAddress?: PostalAddress;
    constructor(options?: {
        countryName?: DirectoryString;
        localityName?: DirectoryString;
        postalAddress?: DirectoryString[];
    });
    static create(options: {
        countryName?: string;
        localityName?: string;
        postalAddress?: string[] | string;
    }): SignerLocation;
    toAsn1(): Asn1BaseBlock;
    static fromAsn1(asn1: Asn1BaseBlock): SignerLocation;
}

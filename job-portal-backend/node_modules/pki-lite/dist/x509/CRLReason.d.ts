import { Asn1BaseBlock, PkiBase } from '../core/PkiBase.js';
/**
 * Represents the reason for certificate revocation.
 *
 * @asn
 * ```asn
 * CRLReason ::= ENUMERATED {
 *       unspecified             (0),
 *       keyCompromise           (1),
 *       cACompromise            (2),
 *       affiliationChanged      (3),
 *       superseded              (4),
 *       cessationOfOperation    (5),
 *       certificateHold         (6),
 *            -- value 7 is not used
 *       removeFromCRL           (8),
 *       privilegeWithdrawn      (9),
 *       aACompromise           (10)
 * }
 */
export declare class CRLReason extends PkiBase<CRLReason> {
    static unspecified: CRLReason;
    static keyCompromise: CRLReason;
    static cACompromise: CRLReason;
    static affiliationChanged: CRLReason;
    static superseded: CRLReason;
    static cessationOfOperation: CRLReason;
    static certificateHold: CRLReason;
    static removeFromCRL: CRLReason;
    static privilegeWithdrawn: CRLReason;
    static aACompromise: CRLReason;
    value: number;
    constructor(value: number);
    toAsn1(): Asn1BaseBlock;
    static fromAsn1(asn1: Asn1BaseBlock): CRLReason;
}

import { asn1js, PkiBase } from '../../core/PkiBase.js';
/**
 * Represents the BasicConstraints extension.
 *
 * @asn
 * ```asn
 * BasicConstraints ::= SEQUENCE {
 *      cA                      BOOLEAN DEFAULT FALSE,
 *      pathLenConstraint       INTEGER (0..MAX) OPTIONAL
 * }
 * ```
 */
export declare class BasicConstraints extends PkiBase<BasicConstraints> {
    cA: boolean;
    pathLenConstraint?: number;
    constructor(options: {
        cA: boolean;
        pathLenConstraint?: number;
    });
    /**
     * Converts the BasicConstraints to an ASN.1 structure.
     */
    toAsn1(): asn1js.Sequence;
    /**
     * Creates a BasicConstraints from an ASN.1 structure
     */
    static fromAsn1(asn1: asn1js.Sequence): BasicConstraints;
}

import { Asn1BaseBlock, ObjectIdentifierString, ParseableAsn1, PkiBase } from '../core/PkiBase.js';
import { AlgorithmIdentifier, ContentEncryptionAlgorithmIdentifier } from '../algorithms/AlgorithmIdentifier.js';
import { ObjectIdentifier } from '../asn1/ObjectIdentifier.js';
import { OctetString } from '../asn1/OctetString.js';
/**
 * Represents encrypted content information.
 *
 * @asn
 * ```asn
 * EncryptedContentInfo ::= SEQUENCE {
 *      contentType ContentType,
 *      contentEncryptionAlgorithm ContentEncryptionAlgorithmIdentifier,
 *      encryptedContent [0] IMPLICIT EncryptedContent OPTIONAL
 * }
 *
 * EncryptedContent ::= OCTET STRING
 * ```
 */
export declare class EncryptedContentInfo extends PkiBase<EncryptedContentInfo> {
    contentType: ObjectIdentifier;
    contentEncryptionAlgorithm: ContentEncryptionAlgorithmIdentifier;
    encryptedContent?: OctetString;
    constructor(options: {
        contentType: ObjectIdentifierString;
        contentEncryptionAlgorithm: AlgorithmIdentifier;
        encryptedContent?: Uint8Array<ArrayBuffer>;
    });
    /**
     * Converts the EncryptedContentInfo to an ASN.1 structure.
     */
    toAsn1(): Asn1BaseBlock;
    /**
     * Creates an EncryptedContentInfo from an ASN.1 structure.
     */
    static fromAsn1(asn1: Asn1BaseBlock): EncryptedContentInfo;
    decrypt(key: string | Uint8Array<ArrayBuffer>): Promise<OctetString>;
    decryptAs<T>(key: string | Uint8Array<ArrayBuffer>, parseAs: ParseableAsn1<T>): Promise<T>;
}

import { Asn1BaseBlock, PkiBase } from '../../core/PkiBase.js';
import { AlgorithmIdentifier } from '../../algorithms/AlgorithmIdentifier.js';
/**
 * Represents a OriginatorPublicKey structure as defined in RFC 5652.
 *
 * @asn
 * ```asn
 * OriginatorPublicKey ::= SEQUENCE {
 *   algorithm AlgorithmIdentifier,
 *   publicKey BIT STRING }
 * ```
 */
export declare class OriginatorPublicKey extends PkiBase<OriginatorPublicKey> {
    algorithm: AlgorithmIdentifier;
    publicKey: Uint8Array<ArrayBuffer>;
    constructor(options: {
        algorithm: AlgorithmIdentifier;
        publicKey: Uint8Array<ArrayBuffer>;
    });
    toAsn1(): Asn1BaseBlock;
    static fromAsn1(asn1: Asn1BaseBlock): OriginatorPublicKey;
}

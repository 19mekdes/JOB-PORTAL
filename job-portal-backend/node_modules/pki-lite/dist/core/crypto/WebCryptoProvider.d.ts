import { AlgorithmIdentifier } from '../../algorithms/AlgorithmIdentifier.js';
import { ObjectIdentifier } from '../../asn1/ObjectIdentifier.js';
import type { PrivateKeyInfo } from '../../keys/PrivateKeyInfo.js';
import type { SubjectPublicKeyInfo } from '../../keys/SubjectPublicKeyInfo.js';
import type { AsymmetricEncryptionAlgorithmParams, CryptoProvider, DerivationAlgorithmParams, HashAlgorithm, SymmetricEncryptionAlgorithmParams, NamedCurve, PbeAlgorithmParams } from './types.js';
/**
 * Cryptographic provider implementation using the Web Crypto API.
 *
 * This provider leverages the browser's native Web Crypto API for cryptographic
 * operations, offering excellent performance and security. It supports modern
 * algorithms including RSA, ECDSA, AES, and SHA hashes. The Web Crypto API is
 * available in both browsers and Node.js (16+), making this provider suitable
 * for cross-platform applications.
 *
 * Supported operations:
 * - Hashing: SHA-1, SHA-256, SHA-384, SHA-512 (MD5 not supported)
 * - Asymmetric: RSA-PKCS1, RSA-PSS, RSA-OAEP, ECDSA, ECDH
 * - Symmetric: AES-GCM, AES-CBC, AES-CTR
 * - Key derivation: PBKDF2, HKDF
 *
 * @example
 * ```typescript
 * const provider = new WebCryptoProvider()
 *
 * // Hash data
 * const hash = await provider.digest(data, 'SHA-256')
 *
 * // Sign data with RSA-PSS
 * const signature = await provider.sign(
 *     data,
 *     privateKeyInfo,
 *     { type: 'RSA_PSS', params: { hash: 'SHA-256', saltLength: 32 } }
 * )
 *
 * // Encrypt with AES-GCM
 * const encrypted = await provider.encryptSymmetric(
 *     plaintext,
 *     key,
 *     { type: 'AES_GCM', params: { iv: iv, tagLength: 16 } }
 * )
 * ```
 */
export declare class WebCryptoProvider implements CryptoProvider {
    /**
     * Reference to the Web Crypto API interface.
     * Protected to allow testing with mock implementations.
     */
    protected crypto: {
        subtle: SubtleCrypto;
    };
    /**
     * Computes a cryptographic hash of the input data.
     *
     * @param data The data to hash
     * @param algorithm The hash algorithm to use
     * @returns The computed hash bytes
     * @throws UnsupportedCryptoAlgorithmError if the algorithm is not supported
     */
    digest(data: Uint8Array<ArrayBuffer>, algorithm: HashAlgorithm): Promise<Uint8Array<ArrayBuffer>>;
    /**
     * Computes an HMAC (Hash-based Message Authentication Code) of the input data.
     *
     * @param key The secret key for HMAC
     * @param data The data to authenticate
     * @param hash The hash algorithm to use
     * @returns The computed HMAC bytes
     * @throws UnsupportedCryptoAlgorithmError if the algorithm is not supported
     */
    hmac(key: Uint8Array<ArrayBuffer>, data: Uint8Array<ArrayBuffer>, hash: HashAlgorithm): Promise<Uint8Array<ArrayBuffer>>;
    /**
     * Converts PKI algorithm parameters to Web Crypto API algorithm specification.
     *
     * @param algorithm The PKI algorithm parameters
     * @returns Web Crypto API compatible algorithm specification
     * @internal
     */
    getWebCryptoAlgorithm(algorithm: AsymmetricEncryptionAlgorithmParams): RsaPssParams | EcKeyImportParams | RsaOaepParams | RsaHashedKeyAlgorithm;
    sign(data: Uint8Array<ArrayBuffer>, privateKeyInfo: PrivateKeyInfo, algorithm: AsymmetricEncryptionAlgorithmParams): Promise<Uint8Array<ArrayBuffer>>;
    verify(data: Uint8Array<ArrayBuffer>, publicKeyInfo: SubjectPublicKeyInfo, signature: Uint8Array<ArrayBuffer>, algorithm: AsymmetricEncryptionAlgorithmParams): Promise<boolean>;
    getWebCryptoSymmetricAlgorithm(algorithm: SymmetricEncryptionAlgorithmParams): AesCbcParams | AesGcmParams;
    getWebCryptoSymmetricKeyGenAlgorithm(algorithm: SymmetricEncryptionAlgorithmParams['type']): AesDerivedKeyParams;
    getWebCryptoDerivationAlgorithm(algorithm: DerivationAlgorithmParams): Pbkdf2Params;
    getRandomValues(length: number): Uint8Array<ArrayBuffer>;
    protected getKeyMaterial(password: string | Uint8Array<ArrayBuffer>): Promise<CryptoKey>;
    protected deriveCryptoKey(password: string | Uint8Array<ArrayBuffer> | CryptoKey, algorithm: PbeAlgorithmParams): Promise<CryptoKey>;
    /**
     * Derives key material using PKCS#12 password-based KDF (RFC 7292 Appendix B).
     * Supports modern hash algorithms for improved security while maintaining OpenSSL compatibility.
     *
     * @param password The password (string or bytes, will be converted to BMPString format)
     * @param salt Salt value for key derivation
     * @param iterationCount Number of iterations for key strengthening
     * @param keyLength Desired key length in bytes
     * @param purpose Key purpose: 'encryption' (id=1), 'iv' (id=2), or 'mac' (id=3)
     * @param hash Hash algorithm to use (default: SHA-1 for legacy compatibility)
     * @returns Promise resolving to the derived key bytes
     *
     * @example
     * ```typescript
     * // Derive a MAC key with SHA-256
     * const macKey = await provider.derivePkcs12Key(
     *     password,
     *     salt,
     *     100000,
     *     32,
     *     'mac',
     *     'SHA-256'
     * )
     * ```
     */
    derivePkcs12Key(password: string | Uint8Array<ArrayBuffer>, salt: Uint8Array<ArrayBuffer>, iterationCount: number, keyLength: number, purpose?: 'encryption' | 'iv' | 'mac', hash?: HashAlgorithm): Promise<Uint8Array<ArrayBuffer>>;
    deriveKey(password: string | Uint8Array<ArrayBuffer> | CryptoKey, algorithm: PbeAlgorithmParams): Promise<Uint8Array<ArrayBuffer>>;
    encryptSymmetric(data: Uint8Array<ArrayBuffer>, key: Uint8Array<ArrayBuffer> | CryptoKey | string, algorithm: SymmetricEncryptionAlgorithmParams | PbeAlgorithmParams): Promise<Uint8Array<ArrayBuffer>>;
    encrypt(data: Uint8Array<ArrayBuffer>, publicKeyInfo: SubjectPublicKeyInfo, algorithm: AsymmetricEncryptionAlgorithmParams): Promise<Uint8Array<ArrayBuffer>>;
    decrypt(data: Uint8Array<ArrayBuffer>, privateKeyInfo: PrivateKeyInfo, algorithm: AsymmetricEncryptionAlgorithmParams): Promise<Uint8Array<ArrayBuffer>>;
    decryptSymmetric(data: Uint8Array<ArrayBuffer>, key: Uint8Array<ArrayBuffer> | CryptoKey | string, algorithm: SymmetricEncryptionAlgorithmParams | PbeAlgorithmParams): Promise<Uint8Array<ArrayBuffer>>;
    generateSymmetricKey(algorithm: SymmetricEncryptionAlgorithmParams): Uint8Array<ArrayBuffer>;
    generateKeyPair(options: {
        algorithm: 'RSA' | 'EC';
        keySize?: number;
        publicExponent?: Uint8Array<ArrayBuffer>;
        hash?: string;
        namedCurve?: string;
    }): Promise<{
        publicKey: Uint8Array<ArrayBuffer>;
        privateKey: Uint8Array<ArrayBuffer>;
    }>;
    getEcCurveParameters(encryptionParams: AsymmetricEncryptionAlgorithmParams): ObjectIdentifier;
    signatureAlgorithm(encryptionParams: AsymmetricEncryptionAlgorithmParams): AlgorithmIdentifier;
    digestAlgorithm(hash: HashAlgorithm): AlgorithmIdentifier;
    keyEncryptionAlgorithm(encryptionParams: AsymmetricEncryptionAlgorithmParams): AlgorithmIdentifier;
    contentEncryptionAlgorithm(encryptionParams: SymmetricEncryptionAlgorithmParams | PbeAlgorithmParams): AlgorithmIdentifier;
    toDerivationAlgorithmParams(algorithm: AlgorithmIdentifier): DerivationAlgorithmParams;
    toPbeAlgorithmParams(algorithm: AlgorithmIdentifier): PbeAlgorithmParams;
    toSymmetricEncryptionAlgorithmParams(algorithm: AlgorithmIdentifier): SymmetricEncryptionAlgorithmParams;
    toAsymmetricEncryptionAlgorithmParams(algorithm: AlgorithmIdentifier, publicKeyInfo?: SubjectPublicKeyInfo): AsymmetricEncryptionAlgorithmParams;
    getEcNamedCurve(algorithm: AlgorithmIdentifier, publicKeyInfo?: SubjectPublicKeyInfo): NamedCurve;
    toHashAlgorithm(algorithm: AlgorithmIdentifier): HashAlgorithm;
}

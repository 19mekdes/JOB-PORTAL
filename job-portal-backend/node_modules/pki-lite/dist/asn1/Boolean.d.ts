import { Asn1BaseBlock, PkiBase } from '../core/PkiBase.js';
/**
 * Represents an ASN.1 BOOLEAN value.
 *
 * @asn
 * ```asn
 * Boolean ::= <value>
 * ```
 */
export declare class Boolean extends PkiBase<Boolean> {
    value: boolean;
    constructor(options: {
        value: boolean;
    });
    toAsn1(): Asn1BaseBlock;
    /**
     * Creates a Boolean from an ASN.1 Boolean structure
     */
    static fromAsn1(asn1: Asn1BaseBlock): Boolean;
    toString(): string;
}

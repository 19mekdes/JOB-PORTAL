import { Asn1BaseBlock, PkiBase } from '../core/PkiBase.js';
/**
 * Represents an ASN.1 TeletexString (T61String) value.
 *
 * @asn
 * ```asn
 * TeletexString ::= <value>
 * ```
 */
export declare class TeletexString extends PkiBase<TeletexString> {
    bytes: Uint8Array<ArrayBuffer>;
    constructor(options: {
        value: string | Uint8Array<ArrayBuffer> | TeletexString;
    });
    toAsn1(): Asn1BaseBlock;
    /**
     * Creates a TeletexString from an ASN.1 TeletexString structure
     */
    static fromAsn1(asn1: Asn1BaseBlock): TeletexString;
    toString(): string;
    /**
     * Converts the TeletexString to a hexadecimal string
     */
    toHexString(): string;
}

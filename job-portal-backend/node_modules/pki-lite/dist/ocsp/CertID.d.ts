import { Asn1BaseBlock, PkiBase } from '../core/PkiBase.js';
import { AlgorithmIdentifier, DigestAlgorithmIdentifier } from '../algorithms/AlgorithmIdentifier.js';
import { OctetString } from '../asn1/OctetString.js';
import { Integer } from '../asn1/Integer.js';
import { Certificate } from '../x509/Certificate.js';
/**
 * ```asn
 * CertID ::= SEQUENCE {
 *     hashAlgorithm           AlgorithmIdentifier,
 *     issuerNameHash          OCTET STRING, -- Hash of issuer's DN
 *     issuerKeyHash           OCTET STRING, -- Hash of issuer's key
 *     serialNumber            CertificateSerialNumber
 * }
 * ```
 */
export declare class CertID extends PkiBase<CertID> {
    hashAlgorithm: AlgorithmIdentifier;
    issuerNameHash: OctetString;
    issuerKeyHash: OctetString;
    serialNumber: Integer;
    constructor(options: {
        hashAlgorithm: AlgorithmIdentifier;
        issuerNameHash: OctetString | Uint8Array | string;
        issuerKeyHash: OctetString | Uint8Array | string;
        serialNumber: Uint8Array | number | bigint | string | Integer;
    });
    toAsn1(): Asn1BaseBlock;
    static fromAsn1(asn1: Asn1BaseBlock): CertID;
    static fromDer(der: Uint8Array<ArrayBuffer>): CertID;
    /**
     * Creates a CertID from issuer and subject certificates.
     *
     * @param issuerCertificate The issuer certificate
     * @param subjectCertificate The subject certificate
     * @param hashAlgorithm Optional hash algorithm (defaults to SHA-256)
     * @returns Promise resolving to the CertID
     */
    static forCertificate(issuerCertificate: Certificate, subjectCertificate: Certificate, hashAlgorithm?: DigestAlgorithmIdentifier): Promise<CertID>;
}

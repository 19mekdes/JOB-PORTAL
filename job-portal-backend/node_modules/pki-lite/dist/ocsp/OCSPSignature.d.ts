import { Asn1BaseBlock, PkiBase } from '../core/PkiBase.js';
import { AlgorithmIdentifier } from '../algorithms/AlgorithmIdentifier.js';
import { Certificate } from '../x509/Certificate.js';
/**
 * Signature ::= SEQUENCE {
 *     signatureAlgorithm   AlgorithmIdentifier,
 *     signature            BIT STRING,
 *     certs             [0] EXPLICIT SEQUENCE OF Certificate OPTIONAL
 * }
 */
export declare class OCSPSignature extends PkiBase<OCSPSignature> {
    signatureAlgorithm: AlgorithmIdentifier;
    signature: Uint8Array<ArrayBuffer>;
    certs?: Certificate[];
    constructor(options: {
        signatureAlgorithm: AlgorithmIdentifier;
        signature: Uint8Array;
        certs?: Certificate[];
    });
    toAsn1(): Asn1BaseBlock;
    static fromAsn1(asn1: Asn1BaseBlock): OCSPSignature;
    static fromDer(der: Uint8Array<ArrayBuffer>): OCSPSignature;
}

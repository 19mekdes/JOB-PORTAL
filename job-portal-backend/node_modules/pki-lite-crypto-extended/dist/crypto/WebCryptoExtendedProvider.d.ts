import { WebCryptoProvider, HashAlgorithm, SymmetricEncryptionAlgorithmParams, AsymmetricEncryptionAlgorithmParams, PbeAlgorithmParams } from 'pki-lite/core/crypto/index.js';
import { SubjectPublicKeyInfo } from 'pki-lite/keys/SubjectPublicKeyInfo.js';
import { PrivateKeyInfo } from 'pki-lite/keys/PrivateKeyInfo.js';
import { AlgorithmIdentifier } from 'pki-lite/algorithms/AlgorithmIdentifier.js';
/**
 * Extended WebCryptoProvider that adds support for additional algorithms
 * such as MD5 hashing, AES ECB mode, and certain PBE algorithms.
 *
 * Note: This implementation uses the 'node-forge' library for RSA encryption/decryption
 * with PKCS#1 v1.5 padding, as WebCrypto does not support this mode directly.
 *
 * Caution: MD5 is considered cryptographically weak and should be used with caution.
 * This provider is intended for compatibility with legacy systems and not for secure applications.
 *
 * @example
 * const provider = new WebCryptoExtendedProvider();
 * const hash = await provider.digest(data, 'MD5');
 * const encrypted = await provider.encryptSymmetric(data, key, { type: 'AES_128_ECB' });
 * const decrypted = await provider.decryptSymmetric(encryptedData, key, { type: 'AES_128_ECB' });
 * const rsaEncrypted = await provider.encrypt(data, publicKeyInfo, { type: 'RSASSA_PKCS1_v1_5' });
 * const rsaDecrypted = await provider.decrypt(rsaEncryptedData, privateKeyInfo, { type: 'RSASSA_PKCS1_v1_5' });
 */
export declare class WebCryptoExtendedProvider extends WebCryptoProvider {
    digest(data: Uint8Array<ArrayBuffer>, algorithm: HashAlgorithm): Promise<Uint8Array<ArrayBuffer>>;
    encryptSymmetric(data: Uint8Array<ArrayBuffer>, key: Uint8Array<ArrayBuffer>, algorithm: SymmetricEncryptionAlgorithmParams): Promise<Uint8Array<ArrayBuffer>>;
    decryptSymmetric(data: Uint8Array<ArrayBuffer>, key: Uint8Array<ArrayBuffer>, algorithm: SymmetricEncryptionAlgorithmParams): Promise<Uint8Array<ArrayBuffer>>;
    encrypt(data: Uint8Array<ArrayBuffer>, publicKeyInfo: SubjectPublicKeyInfo, algorithm: AsymmetricEncryptionAlgorithmParams): Promise<Uint8Array<ArrayBuffer>>;
    decrypt(data: Uint8Array<ArrayBuffer>, privateKeyInfo: PrivateKeyInfo, algorithm: AsymmetricEncryptionAlgorithmParams): Promise<Uint8Array<ArrayBuffer>>;
    toSymmetricEncryptionAlgorithmParams(algorithm: AlgorithmIdentifier): SymmetricEncryptionAlgorithmParams;
    contentEncryptionAlgorithm(encryptionParams: SymmetricEncryptionAlgorithmParams | PbeAlgorithmParams): AlgorithmIdentifier;
}
